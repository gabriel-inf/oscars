<#list vlans as vlan> 
exit all

<#if vlan = "277" >
<#assign hackedVlan = "0"> 

<#elseif vlan = "278">
<#assign hackedVlan = "0"> 


<#if portZ = "6/1/1">
<#assign portZ = "6/1/2"> 
</#if>

<#if portA = "6/1/1">
<#assign portA = "6/1/2"> 
</#if>

<#else>
<#assign hackedVlan = vlan> 
</#if>

configure service vpls ${vlan} customer 1 create sap ${portA}:${hackedVlan} create no shutdown
configure service vpls ${vlan} customer 1 create sap ${portZ}:${hackedVlan} create no shutdown
configure service vpls ${vlan} customer 1 service-mtu 9194
configure service vpls ${vlan} customer 1 stp no shutdown
configure service vpls ${vlan} customer 1 igmp-snooping no shutdown
configure service vpls ${vlan} customer 1 pim-snooping no shutdown
configure service vpls ${vlan} fdb-table-size 4096
configure service vpls ${vlan} no shutdown
</#list>


