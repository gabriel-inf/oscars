<#list vlans as vlan> 
<#if vlan = "277" >

<#if portA = "6/1/1">
<#assign hackedVlanA = "0"> 
<#assign hackedVlanZ = vlan> 
</#if>

<#if portZ = "6/1/1">
<#assign hackedVlanA = vlan> 
<#assign hackedVlanZ = "0"> 
</#if>

<#elseif vlan = "278">

<#if portA = "6/1/1">
<#assign portA = "6/1/2"> 
<#assign hackedVlanA = "0"> 
<#assign hackedVlanZ = vlan> 
</#if>

<#if portZ = "6/1/1">
<#assign portZ = "6/1/2"> 
<#assign hackedVlanA = vlan> 
<#assign hackedVlanZ = "0"> 
</#if>

<#else>
<#assign hackedVlanA = vlan> 
<#assign hackedVlanZ = vlan> 
</#if>

configure service vpls ${vlan} customer 1 create 
exit all 

configure service vpls ${vlan} sap ${portA}:${hackedVlanA} create no shutdown
configure service vpls ${vlan} sap ${portZ}:${hackedVlanZ} create no shutdown
configure service vpls ${vlan} service-mtu 9194
configure service vpls ${vlan} stp no shutdown
configure service vpls ${vlan} igmp-snooping no shutdown
configure service vpls ${vlan} pim-snooping no shutdown
configure service vpls ${vlan} fdb-table-size 4096
configure service vpls ${vlan} per-service-hashing
configure service vpls ${vlan} no shutdown
</#list>


