<#list vlans as vlan> 
<#if vlan = "277" >

<#if portA = "6/1/1">
<#assign hackedVlanA = "0"> 
<#assign hackedVlanZ = vlan> 
</#if>

<#if portZ = "6/1/1">
<#assign hackedVlanA = vlan> 
<#assign hackedVlanZ = "0"> 
</#if>

<#elseif vlan = "278">

<#if portA = "6/1/1">
<#assign portA = "6/1/2"> 
<#assign hackedVlanA = "0"> 
<#assign hackedVlanZ = vlan> 
</#if>

<#if portZ = "6/1/1">
<#assign portZ = "6/1/2"> 
<#assign hackedVlanA = vlan> 
<#assign hackedVlanZ = "0"> 
</#if>

<#else>
<#assign hackedVlanA = vlan> 
<#assign hackedVlanZ = vlan> 
</#if>

configure service vpls ${vlan} customer 1 create sap ${portA}:${hackedVlanA} create no shutdown
configure service vpls ${vlan} customer 1 create sap ${portZ}:${hackedVlanZ} create no shutdown
configure service vpls ${vlan} customer 1 service-mtu 9194
configure service vpls ${vlan} customer 1 stp no shutdown
configure service vpls ${vlan} customer 1 igmp-snooping no shutdown
configure service vpls ${vlan} customer 1 pim-snooping no shutdown
configure service vpls ${vlan} fdb-table-size 4096
configure service vpls ${vlan} no shutdown
</#list>


