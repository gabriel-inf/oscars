configure terminal

vlan ${vlan}
  description ${description}
exit

interface Vlan${vlan}
  no shutdown
  mtu 9174
  description ${description}
exit

interface ${portA}
  switchport trunk allowed vlan add ${vlan}
exit

interface ${portZ}
  switchport trunk allowed vlan add ${vlan}
exit

exit
