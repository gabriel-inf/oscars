configure
delete interface ${portA} unit ${vlan} 
delete interface ${portZ} unit ${vlan} 
delete protocols l2circuit local-switching interface ${portA}.${vlan} 

commit and-quit sync comment "IDC removed local-switching: ${portA}.${vlan} ,  ${portZ}.${vlan}"

