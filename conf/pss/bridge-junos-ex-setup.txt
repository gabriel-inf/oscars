configure private
set vlans vlan${vlan} description "${description}" vlan-id ${vlan}
set interfaces ${portA} unit 0 family ethernet-switching vlan members vlan${vlan}
set interfaces ${portZ} unit 0 family ethernet-switching vlan members vlan${vlan}
commit and-quit sync comment "IDC added VLAN ${vlan} to ${portA}, ${portZ}"
