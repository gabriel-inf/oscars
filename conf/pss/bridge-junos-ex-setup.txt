configure
set vlans vlan${vlan} desc "vlanDESC" vlan-id ${vlan}
set interface ${ifce} unit 0 family ethernet-switching vlan members vlan${vlan}
set interface ae3 unit 0 family ethernet-switching vlan members vlan${vlan}
commit and-quit sync comment "IDC added VLAN ${vlan} to ${port}, ae3"
