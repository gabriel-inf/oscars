configure

edit interfaces ${ifceA}
set unit ${vlan} vlan-id ${vlan}
set unit ${vlan} description ${description}
set unit ${vlan} encapsulation vlan-bridge 
top

edit interfaces ${ifceZ}
set unit ${vlan} vlan-id ${vlan}
set unit ${vlan} description ${description}      
set unit ${vlan} encapsulation vlan-bridge 
top


edit bridge-domains

edit vlan-${vlan}
set vlan-id ${vlan}
set description ${description}
set interface ${ifceA}.${vlan}
set interface ${ifceZ}.${vlan}
top


commit and-quit sync comment "IDC bridged VLAN ${vlan} at ${ifceA}, ${ifceZ}"

