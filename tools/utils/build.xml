<?xml version="1.0" encoding="ISO-8859-1"?>

<project name="idcutil" default="all">

    <property environment="env" />
    <property name="build.home" value="../.." />
    <property name="build.classes" value="${build.home}/build/WEB-INF/classes" />
    <property name="idcutil.home" value="." />

    <path id="compile.path">
        <pathelement location="${build.classes}" />
    </path>

    <target name="all" depends="idcutil" description="Do it all" />

    <target name="clean" description="Delete main build directory">
        <delete dir="${idcutil.home}/classes" />
        <delete dir="${idcutil.home}/lib" />
        <delete failonerror="false">
            <fileset dir="${idcutil.home}" includes="IDCUtil.jar" />
        </delete>
    </target>

    <target name="prepare" description="Create build directoy and copy in files">
        <mkdir dir="${idcutil.home}/classes"/>
        <copy todir="lib">
            <fileset dir="${build.home}/lib" includes="hibernate3.jar,dom4j-*.jar,commons-collections-*.jar,transaction-api-1.1.jar,mysql-connector-java-*.jar,c3p0-*.jar, javassist-3.4.ga.jar, slf4j-api-1.5.2.jar, slf4j-jcl-1.5.2.jar slf4j-simple-1.5.2.jar"/>
            <fileset dir="${build.home}/lib/axis2" includes="log4j-*.jar,commons-logging-*.jar"/>
        </copy>
        <copy todir="classes">
            <fileset dir="${idcutil.home}" includes="log4j.properties" />
        </copy>
    </target>
    
    <target name="compile" description="Compile Java sources" depends="prepare">
        <javac srcdir="." includes="*.java" destdir="${idcutil.home}/classes" classpathref="compile.path">
            <classpath>
                <fileset dir="${build.home}/lib" includes="**/*.jar"/>
                <fileset dir="${build.home}/build/WEB-INF/classes" includes="**/*.jar" />
            </classpath>
        </javac>
    </target>

    <target name="idcutil" description="Create IDCUtil.jar file" depends="compile" >
        <jar destfile="IDCUtil.jar">
            <fileset dir="${build.classes}" includes="**/*.class,**/*.xml" />
            <fileset dir="${idcutil.home}/classes"  includes="*.class,log4j.properties" />
        </jar>
    </target>

</project>


